language: ruby

rvm:
  - ruby-head
  - 2.3.0
  - 2.2.0
  - 2.1.5
  - 2.0.0
  - 1.9.3

services:
  - mongodb
  
before_script:
  - mongo test-db --eval 'db.addUser("test-user", "test-pass");'
  - mongod --version
  
